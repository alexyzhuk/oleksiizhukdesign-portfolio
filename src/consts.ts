// Central place for outbound CTA links used across the site.

export const CALENDLY_URL = "https://calendly.com/alexy-zhuk/30min";

// Powers every "Start a project" button on the site.
// TODO: swap to the real Tally project-inquiry form URL when ready —
// temporarily points to Calendly so the CTA works on the live site.
export const INQUIRY_URL = CALENDLY_URL;
