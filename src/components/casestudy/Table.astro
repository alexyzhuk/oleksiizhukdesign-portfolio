---
interface Column {
    header?: string;
    key: string;
    width?: string;
}
interface Props {
    columns: Column[];
    data: Record<string, string>[];
}
const { columns, data } = Astro.props;
---

<div class="max-w-200 mx-auto">
    <div class="overflow-x-auto">
        <table class="w-full border-collapse">
            <tbody class="flex flex-col gap-1">
                {
                    data.map((row, index) => (
                        <tr class="flex gap-1">
                            {columns.map((column, colIndex) => (
                                <td
                                    class={`py-4 px-6 text-base bg-[#f9fafb14] tracking-[-0.03em] leading-relaxed ${column.width || ""} ${colIndex === 0 ? "w-[40%] shrink-0" : "flex-1"}`}
                                >
                                    {row[column.key]}
                                </td>
                            ))}
                        </tr>
                    ))
                }
            </tbody>
        </table>
    </div>
</div>
